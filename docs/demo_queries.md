# Demo Queries

Use these queries to demo the app and evaluation system.

1. **what is bm25**
   - Expected: "BM25 is a ranking function..."

2. **vector search**
   - Expected: "Vector search uses embeddings..."

3. **hybrid ranking**
   - Expected: "Hybrid ranking combines keyword and semantic scores..."

4. **answer extraction**
   - Expected: "Answer extraction returns the most relevant sentence..."

5. **offline-first search**
   - Expected: "Offline-first search stores data on device..."

6. **inverted index**
   - Expected: "An inverted index maps terms to documents..."

7. **cosine similarity**
   - Expected: "Cosine similarity compares vectors..."

8. **stopwords**
   - Expected: "Stopwords are common words removed..."

9. **chunking**
   - Expected: "Chunking splits documents into passages..."

10. **phrase match**
    - Expected: "Exact match and phrase match signals..."
